#include <string>
#include <iostream>
#include "hello.hpp"

// Main function that will run call the function from hello.cpp, now defined in hello.hpp then terminate.
int main()
{
    // std::stringstream buffer;
    // buffer << getHelloWorld() << std::endl;


    std::cout << "test" << std::endl;
    return 0;
}